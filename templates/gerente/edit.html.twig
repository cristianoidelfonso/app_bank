{% extends 'base.html.twig' %}

{% block title %}Edit Gerente{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Edit Gerente</h1>

        {{ include('gerente/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_gerente_index') }}">back to list</a>

        {{ include('gerente/_delete_form.html.twig') }}
    </div>
{% endblock %}
