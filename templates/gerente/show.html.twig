{% extends 'base.html.twig' %}

{% block title %}Gerente{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Gerente</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ gerente.id }}</td>
                </tr>
                <tr>
                    <th>Nome</th>
                    <td>{{ gerente.nome }}</td>
                </tr>
                <tr>
                    <th>Created_at</th>
                    <td>{{ gerente.createdAt ? gerente.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Updated_at</th>
                    <td>{{ gerente.updatedAt ? gerente.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_gerente_index') }}">back to list</a>

        <a href="{{ path('app_gerente_edit', {'id': gerente.id}) }}">edit</a>

        {{ include('gerente/_delete_form.html.twig') }}
    </div>    
{% endblock %}
