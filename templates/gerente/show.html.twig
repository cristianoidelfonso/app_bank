{% extends 'base.html.twig' %}

{% block title %}Gerente{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Gerente</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ gerente.id }}</td>
                </tr>
                <tr>
                    <th>Nome</th>
                    <td>{{ gerente.nome }}</td>
                </tr>
                <tr>
                    <th>Created_at</th>
                    <td>{{ gerente.createdAt ? gerente.createdAt|date('d/m/Y') : '' }}</td>
                </tr>
                <tr>
                    <th>Updated_at</th>
                    <td>{{ gerente.updatedAt ? gerente.updatedAt|date('d/m/Y') : '' }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_gerente_index') }}">back to list</a>

        <a class="btn btn-sm btn-outline-primary" href="{{ path('app_gerente_edit', {'id': gerente.id}) }}">edit</a>

        {{ include('gerente/_delete_form.html.twig') }}
    </div>    
{% endblock %}
