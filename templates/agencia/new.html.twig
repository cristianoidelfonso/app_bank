{% extends 'base.html.twig' %}

{% block title %}New Agencia{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Create new Agencia</h1>
        
        {{ include('agencia/_form.html.twig') }}
        
        <a href="{{ path('app_agencia_index') }}">back to list</a>
    </div>
{% endblock %}
