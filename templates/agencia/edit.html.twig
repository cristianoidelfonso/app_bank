{% extends 'base.html.twig' %}

{% block title %}Edit Agencia{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Edit Agencia</h1>

        {{ include('agencia/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_agencia_index') }}">back to list</a>

        {{ include('agencia/_delete_form.html.twig') }}
    </div>
{% endblock %}
