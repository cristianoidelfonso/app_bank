{% extends 'base.html.twig' %}

{% block title %}Conta{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Conta</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ conta.id }}</td>
                </tr>
                <tr>
                    <th>Created_at</th>
                    <td>{{ conta.createdAt ? conta.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Updated_at</th>
                    <td>{{ conta.updatedAt ? conta.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Numero</th>
                    <td>{{ conta.numero }}</td>
                </tr>
            </tbody>
        </table>
        
        <div class="d-inline">
            <a class="{{classBtn}} btn-primary col-2" href="{{ path('app_conta_index') }}">back to list</a>

            <a class="{{classBtn}} btn-info col-2" href="{{ path('app_conta_edit', {'id': conta.id}) }}">edit</a>

            {# <a class="{{classBtn}} btn-warning col-2" href="{{ path('app_conta_block', {'id': conta.id}) }}">block</a> #}
           
            {# <form class="{{classBtn}} btn-danger col-2" method="post" action="{{ path('app_conta_delete', {'id': conta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conta.id) }}">
                <button class="">Delete</button>
            </form> #}

            {{ include('conta/_block_form.html.twig') }}
            {{ include('conta/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
